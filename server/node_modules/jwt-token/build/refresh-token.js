"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const dayjs_1 = __importDefault(require("dayjs"));
const jwt_token_1 = require("./jwt-token");
class RefreshToken extends jwt_token_1.JwtToken {
    constructor(config) {
        super(config);
    }
    isAllowedRefresh(refreshTokenOption) {
        const refreshTokenExpireDate = dayjs_1.default.unix(this.decoded.exp);
        if (refreshTokenExpireDate.diff(dayjs_1.default(), refreshTokenOption.refreshRefreshTokenAllowedUnit) <= refreshTokenOption.refreshRefreshTokenAllowedValue) {
            return true;
        }
        else {
            return false;
        }
    }
    refreshRefreshTokenIfPossible(CreaterefreshTokenOption) {
        const isAllowed = this.isAllowedRefresh(CreaterefreshTokenOption.refreshTokenOption);
        if (isAllowed) {
            this.create(CreaterefreshTokenOption);
        }
        return isAllowed;
    }
}
exports.RefreshToken = RefreshToken;
